spring:
  application:
    name: Config-Deafult
eureka:
  instance:
    fetch-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8791/eureka/
